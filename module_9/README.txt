 XML must be in the format:

  <newsfeed>
	<news name="The header must start with new/adv/hor. Can be either full or abbreviated header"> (attribute can't be empty)
		<text>any text</text> (can't be empty)
		<extension>city/expiration date/zodiac sign</extension> (can't be empty)
	</news>
  </newsfeed>

